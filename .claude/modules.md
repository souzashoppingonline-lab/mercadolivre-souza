# Módulos do Sistema

> Escopo: a organização do sistema em **módulos** — cada um com seu próprio menu lateral, como se fosse um sistema separado. Troca-se de módulo pelo **switcher no topbar** (`.module-switcher`). Para o frontend estático em si ver `frontend.md`; para os marketplaces (que são "sub-sistemas" dentro do Operacional) ver `frontend.md`/`shopee.md`/`amazon.md`.

## Os 3 módulos

| Módulo | O que é | Menu lateral | Fonte de dados |
|---|---|---|---|
| **Operacional** | O sistema atual completo (ML + Amazon + Shopee, rankeamento, embalagem, conciliação, etc.) | `js/layout.js` (`NAV_ITEMS`) — e os layouts próprios de Amazon/Shopee dentro dele | Postgres principal (`db/pool.js`) |
| **Financeiro** | Controle financeiro (em construção) | `js/layout-financeiro.js` (`FIN_NAV_ITEMS`) | **Supabase separado** (banco já existente de outra ferramenta — ver abaixo) |
| **Inteligência de Negócio** | Análises estratégicas | `js/layout-bi.js` (`BI_NAV_ITEMS`) | **Postgres principal** (dados operacionais) |

O **Operacional** está 100% pronto. **Financeiro** está em construção (várias telas prontas, ver abaixo). **Inteligência de Negócio** começou pelo **Painel Estratégico** (`pages/inteligencia-negocio.html`).

## Como funciona a troca de módulo

- Cada módulo tem seu **layout próprio** (sidebar + topbar), mesmo padrão de `js/layout-amazon.js`/`js/layout-shopee.js`: a página tem `<div id="app-sidebar">`/`<div id="app-topbar">` e o layout injeta o menu no `DOMContentLoaded`.
- O **switcher de módulos** (`buildModuleSwitcher`, classe CSS `.module-switcher`) fica no `topbar-left` e leva a `../index.html` (Operacional), `financeiro.html` e `inteligencia-negocio.html`. Está replicado em `layout.js`, `layout-financeiro.js` e `layout-bi.js` (mesmo motivo do switcher de marketplace ser replicado — cada layout monta seu próprio topbar) e também inline no `index.html` (topbar da raiz).
## Autorização de módulos (implementado)

Fonte de verdade: o objeto **`MODULES`** em `server/src/routes/staffAuth.js` — mapeia cada módulo para os papéis que podem acessá-lo, mais as `pages`/`apiPrefixes` que pertencem a ele:

```js
const MODULES = {
  operacional: { roles: '*' },                          // todos os papéis logados
  financeiro:  { roles: ['admin'], pages: ['/pages/financeiro.html'], apiPrefixes: ['/api/financeiro'] },
  bi:          { roles: ['admin'], pages: ['/pages/inteligencia-negocio.html'], apiPrefixes: ['/api/bi'] },
};
```

Hoje **Financeiro e Inteligência de Negócio são só `admin`**. Para autorizar um papel novo, mudar quem vê um módulo, ou adicionar um módulo, **edita-se só esse objeto** — nada mais.

Como é aplicado:
- **Backend (gate real)**: `requireStaffAuth` chama `restrictedModuleForPath(path)`; se o path é de um módulo restrito e o papel não está autorizado, bloqueia (`403` em API) ou redireciona pra `/` (página). Vale pra qualquer papel.
- **Frontend (UX)**: `GET /auth/staff/me` devolve `modules: [...]` (chaves autorizadas). `applyModuleAuth()` em `js/layout.js` **remove do switcher** os botões de módulo não autorizados (inclui o switcher inline do `index.html`, via `data-module`). Se o gate estiver desligado (`modules` ausente), não esconde nada.

Papéis restritos legados (`embalagem`, `shopee-demo`) continuam com suas próprias regras (redirecionados antes de chegar ao gate de módulo).

## Módulo Financeiro — banco Supabase (implementado, READ-ONLY)

O Financeiro lê de um **Supabase existente** (projeto "Sistema de Gestão Financeira E-commerce", dados reais de outra ferramenta — **nada pode ser perdido**). Como está feito:

- **Cliente isolado** `server/src/db/supabaseFin.js` — REST (PostgREST `/rest/v1`). Leitura por GET (lista tabelas, prévia, `selectRows`). **Escrita habilitada por allowlist** (`WRITE_ALLOW`, hoje só `sales_entries`): `insertRow`/`updateRow`/`deleteRow` via POST/PATCH/DELETE, `Prefer: return=representation`, update/delete por `?id=eq.<id>`. Qualquer tabela fora da allowlist responde 403. Distinto do `db/pool.js`; `migrate.js` **nunca** aponta pra cá.
- **Config** em `env.financeiro` (`SUPABASE_FIN_URL` + `SUPABASE_FIN_KEY`, do `.env` do servidor). Sem elas o módulo responde "não configurado". A chave pode ser `anon` (respeita RLS) ou `service_role` (ignora RLS — só no servidor).
- **Rotas** `/api/financeiro/*` (montadas em `server.js`, **só admin** via MODULES): `GET /status`, `GET /tabelas`, `GET /tabela/:nome?limit` (prévia ≤200), `GET /dados/:nome?limit&order` (leitura ≤5000). Escrita (allowlist): `POST /dados/:nome`, `PATCH /dados/:nome/:id`, `DELETE /dados/:nome/:id`. Métodos espelho em `js/db.js` (`getFinanceiroStatus/Tabelas/Tabela/Dados`, `addFinanceiroRow/updateFinanceiroRow/deleteFinanceiroRow`).
- **Página** `pages/financeiro.html` = **explorador**: mostra status da conexão, lista as tabelas do Supabase e a prévia de cada uma. Serve pra descobrir o schema real (o ambiente de dev não tem egress pro Supabase, então a validação acontece no deploy).
- **Rede**: o servidor de produção precisa de egress pro Supabase (443 REST). Os dois bancos **não se juntam em SQL** — cruzamento ML×Financeiro é na aplicação.

## Clone do ERP FinanceEcom (Readdy) — referência mestra

O usuário está **migrando** um ERP financeiro (feito no Readdy, React) para cá. A migração agora **grava neste sistema** (não só lê): a escrita é habilitada tela a tela via `WRITE_ALLOW` (a 1ª foi `sales_entries`, pelo formulário "Novo Lançamento Diário" da tela Vendas). **Manter o tema ESCURO e o layout atual do dashboard** — clonar só a **funcionalidade/cálculos**, não o visual claro do Readdy. **A escrita exige a chave `service_role` (`sb_secret`) em `SUPABASE_FIN_KEY`** — com a `publishable` o Supabase recusa por RLS.

- **Memória permanente do ERP**: `.claude/financeiro-clone-guide.md` (guia completo: 28 rotas, componentes, regras) e `.claude/financeiro-supabase-schema.md` (schema real das ~60 tabelas). **Consultar sempre** antes de construir uma tela do Financeiro.
- **Empresas/lojas**: UNIFULL, TOP MIX, R SOUZA, SHOPEE. Campo `empresa` (text) em boletos/receivables/compras; `store_id` (uuid) na maioria.
- **Fórmulas-chave**: Margem contribuição = `gross_revenue − marketplace_fees − subsidized_shipping − cogs − ads_ml − ads_external − tax`. Semáforo margem: >25 verde, 15-25 amarelo, <15 vermelho, <0 prejuízo. Projeção (run rate) = média diária × dias do mês; status ahead>105% / risco 95-105% / atrasado<95%. Break-even = (custos fixos+operacionais) / margem%. DRE: receita → (-)deduções → (-)CMV → lucro bruto → (-)despesas → lucro líquido.
- **Ordem das telas**: Vendas ✅ → Despesas & DRE → DRE → Fluxo de Caixa → Boletos → Projeção → Compras → Home → resto.

### Telas reais (read-only) — em construção

- **Vendas & Custos** (`pages/financeiro-vendas.html`, nav `Vendas & Custos`): lê `sales_entries` + `stores` + `monthly_goals`. **Funcionalidade fiel ao Readdy** (tema escuro nosso): **Meta de Faturamento** (11 cards — Realizado D-1, Meta, Meta Restante, Gap Projeção, Velocidade Média, Projeção Mês, Meta do Dia, Ontem, Pior Dia, Meta Mínima, Dia Ideal — via porta do `useGoalCalculations`), barra de progresso + status ahead/risco/atrasado; **Metas por Empresa** (card por loja: realizado/meta/%/projeção/ritmo/margem); **Indicadores do Período** (Receita, MC, Vendas, Ticket + variação vs mês ant.); **Meta Diária** (dia a dia com semáforo Passou/Faltou, meta = goal/dias); **Composição da Receita Bruta** (donut + 7 cards: Taxas MP/Frete/CMV/Ads ML/Ads Ext./Imposto/Margem, cada um % sobre a receita); **Evolução Diária da Margem %** (linha + média tracejada + badges Média/Melhor dia/Pior dia + zonas + nota de tendência p.p. + tooltip com Margem %/Margem R$/Rec. Bruta); **Comparativo por Empresa** (barras agrupadas receita/custos/margem por loja); **Composição dos Custos** (donut + abas Todas/por-loja, cards CMV/Frete/Tarifas/Imposto/Ads/Margem % sobre a receita); **Volume vs. Rentabilidade** (combo por lançamento: barras Rec. Bruta cinza + Margem R$ verde/vermelha + linha Margem % no eixo direito, badges Total Rec./Total Margem/% geral, cards Maior volume/Maior margem R$). **Importar planilha (MercadoTurbo)**: card com seletor de **loja** (escolhido antes) + upload `.xlsx` (SheetJS via CDN, parse no navegador). Agrega os **pedidos PAGOS por dia** (`Status Pedido='Pago'`, exclui Cancelado) → `quantity_sales`=contagem, `gross_revenue`=Σ Faturamento ML, `marketplace_fees`=Σ Tarifa de Venda, `subsidized_shipping`=Σ Frete Vendedor (Frete Comprador **não** entra), `cogs`=Σ Custo, `tax`=Σ Imposto (mapeia colunas por nome do cabeçalho; a margem resultante bate com a MC da planilha). O **ADS não vem na planilha** → modal de confirmação mostra o preview de cada dia e pede o ADS (por dia) antes de gravar. Grava em `sales_entries` (insert; se já existe lançamento no dia+loja, **substitui** via `updateFinanceiroRow`). **Formulário "Novo Lançamento Diário"** (escrita): grava uma linha em `sales_entries` (data, qtd, receita, taxas, frete, CMV, ads ML, ads externos, imposto, loja) — 1ª tela com escrita da migração. **Ao clicar "Lançar" abre um modal de confirmação** (empresa + data + valores + margem calculada) para evitar lançamento errado. **Tabela "Histórico de Lançamentos"** (linha a linha por empresa, respeita filtros mês/ano/loja) com botões **editar** (corrigir data/valores → `updateFinanceiroRow`, PATCH `?id=eq.`) e **excluir** (com confirmação → `deleteFinanceiroRow`); borda esquerda colorida pelo semáforo de margem. Tudo reflete na tela sem recarregar.
- Meta geral = linha de `monthly_goals` com `store_id` NULL (igual ao Readdy `MonthlyGoal`); nunca soma das metas por loja (evita duplicar). Fallback: se não houver linha geral, soma as por-loja.
- Rotas: `GET /api/financeiro/dados/:nome?limit&order` (≤5000, order `col.asc|desc`) + `POST/PATCH/DELETE /api/financeiro/dados/:nome[/:id]` (allowlist). `js/db.js`: `getFinanceiroDados`, `addFinanceiroRow`.

- **Despesas & DRE** (`pages/financeiro-despesas.html`, nav `Despesas & DRE`): lê `expenses` + `expense_categories` + `sales_entries`. **Parcelamento/Recorrência (form Nova Despesa):** ao ligar "Recorrente mensal", modal (`onRecToggle`) pergunta **em quantas parcelas/meses** + checkbox **"dividir o valor total pelas parcelas"** + **"gasto fixo mensal"**. **Parcelar CRIA N lançamentos** (um por mês a partir da competência, `addMonths`/`dateForComp`, rótulo `(i/N)`, `doInsertFlow` faz N inserts) — corrige o gap em que a recorrência só guardava um número sem aparecer nos outros meses. Cada linha grava `recorrencia_vezes_mes` (int) e `gasto_fixo_mensal` (bool), colunas aditivas no Supabase (migration `expenses_recorrencia_vezes_e_gasto_fixo`, via MCP). **Competência editável no modal de confirmação** (`aplicarComp`): selects mês/ano (default = filtro do topo) pra ajustar quando a dívida cai em outra competência — ex.: compra no **cartão de crédito** que entra na fatura seguinte; recalcula `month`/`year`/`date` do lançamento. **No modal Editar** (`editarDespesa`/`doUpdate`): competência editável + bloco inline (`toggleERecBox`) com parcelas/meses e "gasto fixo mensal". Se marcar recorrente com N>1, oferece (confirm) **criar as N-1 parcelas dos meses seguintes** — permite pegar um lançamento já feito e gerar as parcelas que faltam. **2 abas**: "DRE & Despesas" e "Ponto de Equilíbrio". Escrita (allowlist) em `expenses` e `expense_categories`. Conteúdo (clone fiel do Readdy, tema escuro):
  - **Lançamentos do mês** = custos "Auto" das vendas (Receita Bruta como entrada; Taxas/Frete/CMV/Ads/Impostos como custo — não editáveis, vêm da tela Vendas) **+** despesas manuais (editáveis/excluíveis). `expenses` tem colunas `type` ('fixed'/'operational'), `description`, `category_id`, `value`, `date`, `is_recurring`, `month`, `year`, `store_id`.
  - **Nova Despesa**: tipo (segmented Fixo/Operacional), categoria, descrição, valor, recorrente — com **modal de confirmação**. Competência = mês/ano selecionados.
  - **DRE Detalhada**: cascata Receita → custos automáticos → Margem de Contribuição → despesas fixas/operacionais → Lucro Líquido, + cards-resumo à direita (Receita, MC com barra, Total Despesas, Lucro Líquido/Prejuízo) + badge de status da margem.
  - **DRE Resumida anual** (painel lateral): Entradas/Custos/Resultado por mês + totais.
  - **Custo por Categoria** (donut + lista rankeada com % e barras, filtro Todos/Fixos/Operacionais).
  - **Tendência** (barras gastos × linha faturamento, 3M/6M/12M + cards mensais com % do faturamento).
  - **Comparativo Mês a Mês por Categoria** (tabela com variação % + Δ absoluto vs mês anterior, "novo" na 1ª aparição).
  - **Ponto de Equilíbrio** = (custos fixos+operacionais) ÷ margem de contribuição %; **a margem usa os últimos 30 dias de vendas** (não o mês). **Ranking de Impacto no Break-even**: quanto cada categoria reduz o break-even se eliminada (= custo ÷ margem%), + destaque "Maior alavanca de redução".
  - Ações: Exportar CSV, Categorias (CRUD categorias), Recorrentes (lista), Limpar Despesas (só as manuais do mês, com confirmação).
- Escrita liberada: `WRITE_ALLOW` em `supabaseFin.js` = `sales_entries`, `expenses`, `expense_categories`.

- **DRE — Resultado** (`pages/financeiro-dre.html`, nav `DRE — Resultado`): read-only, lê `sales_entries` + `expenses` + `expense_categories` (mesmas 3 tabelas). Seções (clone fiel do `/dre` do Readdy, tema escuro):
  - **Visão Anual**: 12 cards de mês (receita + resultado, mês atual destacado) clicáveis para trocar o mês do DRE; + Receita Anual / Custos Totais / Lucro Acumulado.
  - **Comparativo** mês vs mês anterior: cards Período, Receita Total, Saída Total, Margem Contrib. (badge de status), Lucro Líquido (badge Prejuízo/Atenção), cada um com % de variação + mini-sparkline SVG (últimos 6 meses).
  - **DRE completa**: cascata Receita → custos operacionais (cada linha com % da receita) → Total Custos Op. → Margem de Contribuição → Despesas (cada despesa individual) → Total Despesas → Lucro Líquido; + **Composição da Receita** (barras) + boxes Total Custos / Resultado.
  - **Projeção Anual**: método **Média 3 meses / Tendência linear (regressão) / Último mês**; cards Receita/Custos/Lucro projetados (fechado vs projetado) + médias mensais; gráfico Receita×Lucro por mês; **Detalhamento Mensal** (tabela com status Fechado/Estimado/Projetado + Total do ano com selo Crítico/Saudável).
  - Ações: Atualizar, **Exportar Excel** (.xls via HTML table) e **Exportar PDF** (janela de impressão).

- **Fechamento Mensal** (`pages/financeiro-fechamento-mensal.html`, nav `Fechamento Mensal`): consolida e TRAVA o resultado de cada mês em `monthly_closing` (a mesma tabela que `financeiro-projecao-caixa.html` já lia — agora esta página também escreve). Diferente de todas as outras páginas do Financeiro, **não recalcula no `<script>`**: lê de `GET /api/financeiro/fechamento/*` (`server/src/routes/financeiro.js`), que chama `financeiroCalc.getFechamentoMensal()` — reusa `salesAgg`/`expTotals`, as MESMAS funções de `getDRE()` (nunca uma 4ª cópia da fórmula de DRE — `financeiro-despesas.html`/`financeiro-projecao-caixa.html`/`financeiro-dre.html` já são 3; ver `decisions.md`).
  - **Fluxo de estado**: `open → in_progress → closed`, e `closed → open` via "Reabrir" (sem checagem extra de papel — o módulo Financeiro inteiro já é admin-only). Mês **aberto/em andamento** mostra os números **ao vivo** (recalculados a cada leitura); mês **fechado** devolve o snapshot gravado em `report_data`, nunca recalcula (é a fonte oficial, não pode mudar sozinha se alguém editar um lançamento antigo depois).
  - **Tela**: seletor de ano + 5 summary cards (Ano Fiscal, Receita Fechada, Margem Contrib., Lucro Líquido, Vendas — só meses `closed`) + grade de 12 meses (clicável) + histórico + legenda das regras. Clicar num mês abre o modal: 4 KPIs, **checklist de 8 itens** (`vendas`/`despesas`/`fluxo_caixa`/`boletos`/`sku`/`dre`/`ads` obrigatórios + `estoque_cmv` opcional — validado no BACKEND também, nunca só na tela), observações (500 car.), ações por estado (Iniciar/Finalizar/Reabrir), relatório em 6 abas (Resumo/DRE/Custos/Boletos/Categorias/Observações), exportação CSV (Blob) e PDF (`window.open`+`window.print()`, mesmo padrão sem lib nova de `financeiro-dre.html`/`bi-rankeamento.html`).
  - **Regra do último dia útil**: só finaliza a partir do último dia útil do mês (sem feriados — calendário não implementado, ver `decisions.md`) — é "a partir de", não "só nesse dia exato" (senão um mês em atraso nunca mais poderia ser fechado depois que o dia passasse).
  - **E-mail ao finalizar**: via **Resend** (`resendClient.js`, mesma credencial `.env` de todo relatório do sistema — `server/src/fechamentoMensalEmail.js`), **não** o EmailJS com destinatários hardcoded do app original. Falha de e-mail nunca desfaz o fechamento já gravado (mesmo racional de `sendReportEmail` no worker).
  - Checklist e observações vivem dentro de `monthly_closing.report_data`/`.notes` — não há coluna própria de checklist no schema (banco gerenciado fora deste repositório, sem migration possível daqui).
  - Escrita liberada: `WRITE_ALLOW` += `monthly_closing` (novo `supabaseFin.upsertRow`, upsert por `month,year` via `Prefer: resolution=merge-duplicates`).

- **Boletos** (`pages/financeiro-boletos.html`, nav `Boletos`): contas a pagar do mês. Lê/grava `boletos_mensais` + `receivables` + `cartoes` + `boleto_categories`. Filtro por empresa (UNIFULL/TOP MIX/R SOUZA/SHOPEE)/mês (`mes_referencia` 'YYYY-MM')/status; **tabs por tipo** (boleto/cartão/imposto/pessoal/fatura ML/flex/custo fixo/variável). KPIs (Total/Pago/Pendente/Atrasado — "atrasado" = pendente com `date` < hoje). **Form "Cadastrar Dívida"** (fiel ao Readdy): tipo segmentado (8), Vencimento/Categoria/Fornecedor/Empresa(+chips)/NF(`numero_nf`, grava só se preenchido)/Nome/Valor/Status segmentado. **Fluxo não-cartão** = 1 INSERT em `boletos_mensais` (`is_origem:true`). **Fluxo cartão** = bloco extra (cartão + parcelas 1-24 + valor total, mostra "Nx de R$Y") → INSERT dívida-mãe (`tipo:'cartao'`,`valor_total`,`numero_parcelas`,`cartao_id`) + N `parcelas_cartao` com vencimentos pela **regra de cartão** (`calcVencimentos`: compra ≤ dia de fechamento → 1ª parcela na fatura do mês, vence no seguinte; depois do fechamento → começa na fatura seguinte). **Gerenciar Cartões** (CRUD `cartoes` + ativar/excluir) e **Recorrentes** (CRUD `boletos_recorrentes` + "Gerar {mês}" pulando duplicatas). Tabela com coluna NF, pagar (toggle `status`)/editar/excluir. **Compras de cartão não aparecem como linha** — a compra-mãe (`tipo='cartao'`,`is_origem`) fica escondida e a tabela mostra **1 fatura virtual por cartão** (soma das `parcelas_cartao` que vencem no `fatura_mes` selecionado) com botão **Pagar Fatura** (quita todas as parcelas pendentes do mês + INSERT `fatura_pagamentos`). KPIs/total excluem a compra-mãe para não somar dobrado. **Explicação detalhada: `.claude/financeiro-cartao-faturas.md`.** **Barra de filtros** (pílulas coloridas): mês + status em abas (Todos/Pendente/Pago/Atrasado) + contador; Dia/Fornecedor/Cartão/Banco/Empresa; NF (busca) + Tipo (abas coloridas por tipo). **Resumo por Empresa** (total/pago/resta) + **Calendário de Vencimentos** (grade do mês com bolinhas pendente/pago/recebível + cards Total Boletos/Recebíveis/Atrasados/Saldo do Mês). Bloco de Recebíveis e Cartões (CRUD `cartoes`). CSV.
- **Recebíveis** (`pages/financeiro-recebiveis.html`, nav `Recebíveis`): lê/grava `receivables`. Agrupado por dia (total + quebra por empresa), abas Todos/Pendentes/Recebidos, filtro empresa/mês, cadastro inline, marcar recebido (toggle `recebido`/`data_recebimento`), CSV/PDF. Regra: só pendentes contam nos indicadores.
  - **Trava 1 lançamento por empresa/dia (v80):** cada empresa entra **uma única vez por data**. Vale no cadastro **e** na edição (mover um lançamento para um dia/empresa já ocupado também é bloqueado); a comparação normaliza data (`slice(0,10)`, aceita timestamp) e empresa (trim + caixa alta). A mensagem de bloqueio mostra o lançamento que já existe (descrição + valor) e aponta a saída. Embaixo do formulário há um aviso do dia escolhido — quem já foi lançado e quais empresas ainda estão liberadas — então a trava aparece **antes** da tentativa, não só como erro. A checagem é na tela, sobre as 5000 linhas que a página já carrega; a garantia definitiva seria um índice único `(empresa, date)` no Supabase (ver `known-bugs.md`).
  - **Editar lançamento (v80):** botão ✏️ em cada linha abre a própria linha como formulário (data, empresa, descrição, valor) com Salvar/Cancelar → `updateFinanceiroRow`. Antes só dava pra marcar recebido ou excluir.
- **Relatório de Cartão** (`pages/financeiro-cartoes.html`, nav `Relatório de Cartão`): centro de controle de cartão de crédito. Lê/grava `parcelas_cartao` (principal) + `cartoes` + `boletos_mensais` (compras) + `parcela_tags` + `fatura_pagamentos`. Filtros globais: mês (`fatura_mes`) + chips de cartão (multi-seleção). **6 sub-abas**: (1) **Cards por Cartão** — card expansível por cartão com status da fatura atual (aberta/fechada/vencida/paga), valores (total/pago/aberto), barra de limite, histórico de faturas expansível (parcela a parcela), botão **Pagar Fatura** (marca todas pendentes + INSERT `fatura_pagamentos`), editar limite, toggle parcela; (2) **Visão Geral** — 4 KPIs (Comprometido/Aberto/Pago/Nº Compras), gráfico gasto por cartão (barras↔pizza), limite usado×disponível (barras empilhadas), comprometimento mensal; (3) **Por Cartão** — tabela drill-down por cartão + editar parcela; (4) **Categorias** — ranking por categoria da compra; (5) **Parcelas Futuras** — agrupadas por fatura, vencidas com badge VENCIDA, filtro status, atribuir tag, toggle/editar; (6) **Tags** — CRUD de tags (cores automáticas), stacked bar por mês, matriz tag×mês, distribuição, CSV/PDF. Escrita: toggle status, pagar fatura, editar limite/parcela, tag_id, CRUD tags.
- Escrita liberada agora: `WRITE_ALLOW` += `boletos_mensais`, `receivables`, `boleto_categories`, `cartoes`, `parcelas_cartao`, `parcela_tags`, `fatura_pagamentos`.
- **Simplificação v1 Cartão:** excluir compra (que deveria DELETE `parcelas_cartao`+`cash_flow_entries`+`boletos_mensais`) ainda não implementado aqui; o "Pagar Fatura" faz INSERT `fatura_pagamentos` + UPDATE parcelas, mas não atualiza `boletos_mensais` da dívida-mãe ainda.
- **Categoria × Fornecedor (v82):** `boleto_categories` guarda **duas coisas na mesma tabela**, separadas pela coluna `type` (`'categoria'` ou `'fornecedor'`). A tela lê tudo e separa em `CATEGORIAS`/`FORNS` na carga — sem esse split o select de **Categoria** listava fornecedor (MARTINS ATACADO, SANTANDER, QUATREE…) junto com Aluguel/Imposto. **Fornecedor virou select** (era campo de texto livre), alimentado só pelos `type='fornecedor'`, com botão **＋** ao lado que abre o mesmo modal de cadastro já no tipo certo. O fornecedor continua gravado **por nome** em `boletos_mensais.supplier` (não por id), então o `value` da option é o nome; um fornecedor antigo que não esteja mais no cadastro entra como opção extra no modal de edição, pra editar um boleto não apagar o que já estava gravado. O filtro de fornecedor do topo passa a unir cadastrados + os já usados nos boletos. Cadastro recusa nome duplicado dentro do mesmo tipo (ignorando maiúsculas), mas permite o mesmo nome em tipos diferentes.
- **Sincronização com Fluxo de Caixa (implementada):** ao marcar um boleto como **pago**, abre o **modal "De qual conta saiu o pagamento?"** (`abrirModalBanco`) — lista as `contas_bancarias` ativas, filtradas pelas contas da empresa do boleto via `conta_empresas`. Ao confirmar: `UPDATE boletos_mensais status='pago'` + cria uma saída em `cash_flow_entries` (`type='expense'`, `boleto_id`, `reason=nome — fornecedor`, `value`, `date`, `category`, `store_id`, `empresa`, `conta_bancaria_id`, `nota_fiscal`). Ao voltar pra **pendente** ou **excluir**, a saída é removida (idempotente: DELETE por `boleto_id` antes de inserir). **Pagar Fatura de cartão** também lança a saída (`category='Cartão de Crédito'`). Filtro novo no backend: `GET /api/financeiro/dados/:nome?filtro=coluna=eq.valor` (`supabaseFin.selectRows`).
- **Simplificações v1 (a refinar):** escrita de NF depende do nome real da coluna em `boletos_mensais` (só grava se preenchido); edição/exclusão inteligente de compra de cartão (preservar parcelas pagas) ainda não na tela Boletos (gestão fica no Relatório de Cartão).

- **Fluxo de Caixa** (`pages/financeiro-fluxo-caixa.html`, nav `Fluxo de Caixa`): lê/grava `cash_flow_entries` (central) + `cash_flow_categories` + `cash_flow_recurring` + `stores` + `contas_bancarias` + `conta_empresas`. **Semáforo** (Saldo acumulado histórico, Entradas/Saídas do mês vs mês ant., Cartão no mês, Hoje/Saldo do mês). **Form** Entrada/Saída (toggle verde/vermelho; saída exige categoria) com edição. **Tabela** de lançamentos (filtros Todos/Entradas/Saídas/Boletos/Cartões + categoria; "Hoje" pinado; linhas de hoje/cartão/boleto destacadas) editar/excluir. **Resumo por Empresa** e **por Conta Bancária** (entradas/saídas/resultado). **Gráfico** de evolução (7/15/30/60 dias). **Resumo Semanal**. Modais: **Categorias**, **Contas Bancárias** (gestão completa — ver abaixo), **Recorrentes** (CRUD `cash_flow_recurring` + "Gerar mês" pulando duplicatas). CSV. Escrita: `WRITE_ALLOW` += `cash_flow_entries`, `cash_flow_categories`, `cash_flow_recurring`, `contas_bancarias`, `conta_empresas`.
  - **Contas Bancárias — gestão (v80):** o modal 🏦 antes só **adicionava e excluía**, o que forçava excluir+recriar pra corrigir um nome (deixando órfão o `conta_bancaria_id` dos lançamentos já feitos) e não tinha onde dizer de qual empresa a conta é. Agora tem: **editar** nome/banco/agência/conta (renomear preserva todo o histórico — é como se troca "Sicoob" por "Santander UNIFULL"), **ativar/inativar** (`ativa`, em vez de excluir), **chips de empresa** por conta gravando/apagando em `conta_empresas` (UNIFULL/TOP MIX/R SOUZA/SHOPEE), **contagem de lançamentos** por conta e **confirmação avisando quantos lançamentos ficam sem conta** antes de excluir. O cadastro de conta nova já aceita as empresas na criação — **sem esse vínculo a conta não aparece** no modal "de qual conta saiu o pagamento?" da tela de Boletos, que filtra por `conta_empresas`.
  - v1 a refinar (prints do usuário): colunas separadas Empresa/Banco/NF na tabela, donut "Saídas por Categoria", gráficos "Distribuição/Share por Empresa", barras de progresso no Resumo Semanal, Total Geral consolidado, e a coluna `conta_bancaria_id`/`empresa` em `cash_flow_entries` (gravadas só se preenchidas — confirmar nome real). "Cartão no mês" é heurístico (reason/category ~ fatura/cartão) até haver flag dedicada.

- **Projeção de Caixa** (`pages/financeiro-projecao-caixa.html`, nav `Projeção de Caixa`): projeção do caixa dia a dia (read; grava só `manual_cash_flow_values`). Lê `cash_flow_entries` (caixa atual = todas income−expense), `receivables` (entradas futuras pendentes), `boletos_mensais` (saídas não-pagas, `tipo!='cartao'`), `parcelas_cartao` (faturas não-pagas), `expenses` (custo fixo/operacional do mês), `cash_flow_recurring` (entradas recorrentes), `sales_entries` (margem de contribuição), `monthly_closing` (faturamento/lucro anual), `manual_cash_flow_values` (input manual). Período 7/15/21/30/45/60d. Seções: Resumo da Movimentação (entradas/saídas/saldo final), 4 KPIs (Caixa Atual, Entradas/Saídas Previstas, Caixa Projetado), **Projeção por Período** (barras coloridas por saúde), **Alertas Inteligentes** (caixa negativo, vencimentos 7d, queda, maior entrada/saída, pico, saudável), **gráfico** saldo/entradas/saídas, **Tabela Diária** (entradas real/proj., saídas, saldo, badges de eventos clicáveis → modal com link p/ Boletos/Recebíveis/Cartão; editar média diária projetada), **Custos & Margem** (dias +/−, variação, cobertura, custo fixo/oper %, margem de contribuição), **Seção Anual** (tabela 12 meses editável: caixa dia 1/entrada/saída banco → `manual_cash_flow_values`; faturamento/**Lucro Mensal (R$)**/Lucro Líq. % do `monthly_closing` — Lucro Líq. % prioriza `dre_data.net_pct`, cai pra `contribution_margin_pct` (marcado com `≈`) só quando o fechamento não trouxe DRE, nunca o contrário; Lucro Mensal R$ = faturamento × essa mesma %, ver `decisions.md`; + gráfico caixa×faturamento×lucro%). Escrita: `WRITE_ALLOW` += `manual_cash_flow_values`, `forecast_starting_balance`.
  - **Base da projeção de entradas** (barra de controle na Projeção Diária): **Média 7 dias de recebíveis** (padrão) / Média 30 dias do fluxo (comportamento antigo) / Valor manual. A média de 7 dias soma os `receivables` numa janela de **7 dias corridos terminando no último dia com recebível lançado** — não em hoje, senão os dias vazios depois do último lançamento derrubariam a média — e **divide por 7 sempre**: fim de semana sem recebimento faz parte da média semanal real. Sem nenhum recebível lançado, cai sozinha na média do fluxo e avisa na tela.
  - **Corte da projeção**: o dia projetado só existe **depois do último recebível lançado**. Antes desse corte, dia sem recebível fica **zerado** — antes, um buraco no meio dos recebíveis já lançados era preenchido com média, o que inventava dinheiro que não ia entrar.
  - **Contador de tendência (%)**: stepper −/+ (passo 5, aceita digitar) que multiplica a média projetada — `projetado = base × (1 + ajuste)`. Serve pra simular aumento ou queda nas vendas. Trava em **−100%** (projeção zero, nunca negativa) e a tabela diária carimba o % no selo *proj.*. É ajuste plano, não composto por dia: o mesmo valor vale pra toda a janela.
  - v1: "Caixa Projetado" usa média diária de entradas (últimos 30d) nos dias sem recebível real (editável na tela, override em memória — não persiste ainda); saldo inicial via `forecast_starting_balance` é fallback não aplicado ainda.

- **Compras & CMV** (`pages/financeiro-compras.html`, nav `Compras & CMV`): clone do `/compras-cmv` do Readdy. A página original tem **9 abas**; esta entrega traz as **3 que não dependem de XML/Storage** — as outras 6 estão no lugar, desabilitadas, e explicam o que falta ao serem clicadas.
  - **Filtro de período (aba Compras)**: **Últimos 30 dias / Mês atual / Total de compras**, abrindo sempre no **mês atual** — é o que se olha no dia a dia; o total existe pra conferir o acumulado, não pra ser o padrão. Recorta os cards (incluindo o de NF-e), a lista, o resumo e o CSV; os cards por empresa também respeitam o período, mas **não** o filtro de empresa (senão as outras zerariam e o comparativo perderia sentido). O botão marcado é sincronizado a partir do estado (`syncBotoesPeriodo`), não da classe escrita no HTML, pra marcação e recorte não divergirem. As abas **Relatórios** e **Comparação** têm recorte próprio (histórico e mês a mês) e não seguem este filtro.
  - **NF-e / Central Fiscal** (bloco 2 da aba Compras, era "Registro rápido"): além da NF-e de mercadoria, lança **qualquer documento que a empresa paga**, com **5 categorias** em botões (mesmo padrão do seletor de empresa): **Documentos Fiscais** (notas e documentos de pagamento, inclusive os **sem chave de acesso** — aluguel, água, luz), **Despesas** (consolidação de gastos), **Ativos** (bens e equipamentos), **Serviços** (NFS-e) e **Fretes** (CT-e). Cada uma explica o que é ao ser escolhida. A categoria é gravada em **`compras_cmv.tipo_compra`** (coluna text que já existia e era sempre `'normal'`) — **sem schema novo**; `'normal'` segue sendo "mercadoria sem categoria fiscal". A importação de XML entra como `documentos`.
  - **Comprovante anexado**: botão *Anexar comprovante* aceita **XML, PDF, PNG, JPG ou WEBP até 20 MB**. XML cai direto no fluxo de importação de NF-e (que já extrai tudo); qualquer outro arquivo sobe pro **Storage do Supabase** e o caminho é gravado em **`compras_cmv.xml_url`**. Um upload falho não deixa anexo pendurado e a tela mostra o motivo.
    - **Por que Storage e não coluna**: texto cabe numa coluna (é o que `compras_xml.xml_original` faz com o XML), binário não — base64 numa linha inflaria a tabela e o payload de toda leitura. O backend ganhou `supabaseFin.uploadObject/downloadObject` falando com a **Storage API** (`/storage/v1`, endpoint diferente do PostgREST), bucket `documentos-fiscais` (configurável em `SUPABASE_FIN_BUCKET`), **criado sozinho na primeira subida**. Rotas: `POST /api/financeiro/arquivo` (multipart, memória — o arquivo só passa a caminho do Storage, não fica no disco do servidor) e `GET /api/financeiro/arquivo/*` (o bucket é **privado**; quem serve é o servidor, com a chave dele, atrás do gate de admin). Espelho em `js/db.js`: `uploadFinanceiroArquivo`, `financeiroArquivoUrl`.
  - **Aba Relatório Fiscal**: análise **só dos documentos categorizados** (mercadoria sem categoria fica de fora de propósito — quem analisa isso é a aba Relatórios). Filtros de mês/ano (ou ano inteiro), categoria e empresa. Cards (documentos, valor, quantos têm comprovante, categorias usadas), rosca + barras por categoria, ranking por fornecedor e a tabela do período com link pro comprovante (ou selo NF-e, ou aviso de *sem comprovante*). Exporta **CSV** e o **relatório mensal em PDF** (janela de impressão, mesmo padrão do resto do projeto).
  - **Aba Compras**: regra de ouro do Readdy — **só registra depois de escolher a empresa** (UNIFULL azul / TOP MIX verde / R SOUZA âmbar, do `constants.ts`); sem empresa o botão fica travado e um aviso aparece. Cards de resumo (total geral + uma por empresa) e **card NF-e** (valor da nota × valor pago × diferença × "economia" %, só aparece quando há compra com `valor_nfe`). **Registro rápido** (fornecedor com datalist, valor, data da compra, data de entrega, observação, status) grava em `compras_cmv` com `origem='MANUAL'`. **Histórico** com filtro por empresa, selo NF-e nas linhas importadas, alternar pendente⇄entregue, editar (modal) e excluir. **Resumo consolidado** (comprado/entregue/pendente + barras por empresa) e export CSV.
  - **Aba Relatórios**: 4 sub-abas — por mês (barras + tabela com ticket médio e % do período), por fornecedor (rosca top 10 + participação + tabela com detalhe por fornecedor), por empresa (rosca + tabela) e divergência NF-e (nota × pago, ordenado pela maior diferença).
  - **Aba Comparação**: cruza 4 fontes no mês — `compras_cmv`, `sales_entries` (CMV e faturamento), `boletos_mensais` e `parcelas_cartao`. **Obrigações = boletos do mês com `tipo != 'cartao'` + parcelas com `fatura_mes` do mês** — o boleto do tipo `cartao` é a dívida-mãe, contar os dois dobraria o valor. Indicadores: cobertura de estoque (`compras ÷ CMV`), variação (`compras − CMV`), eficiência de giro (`CMV ÷ compras`), pressão (`obrigações ÷ CMV`) com anel de saúde (≤1,1× saudável / ≤1,5× atenção / >1,5× crítico) e diagnóstico em texto. Modo "comparar meses" mostra delta % em cada métrica; export CSV. **É sempre do negócio inteiro** — `sales_entries` não tem coluna `empresa`, então CMV/faturamento não se separam por empresa como as compras (avisado na própria tela).
  - **Fornecedores vêm de `boleto_categories` com `type='fornecedor'`** (é o que o `useCmvData` do Readdy usa), não de `fornecedores_cmv` — essa é do fluxo legado de compra item-a-item.
  - Escrita: `WRITE_ALLOW` += `compras_cmv`. A tabela **já existe** no Supabase com as colunas fiscais completas (`chave_nfe`, `numero_nf`, `valor_nfe`, `valor_pago`, `diferenca_nfe`, `v_icms`/`v_ipi`/`v_pis`/`v_cofins`/`v_frete`/`v_st`/`v_desconto`, `xml_url`, `origem`).
  - **Importar XML da NF-e** (botão na aba Compras): aceita **vários arquivos de uma vez**. O parse é no **navegador** (`DOMParser`, sem lib nova — mesmo padrão da importação da planilha MercadoTurbo). De cada nota tira chave (44 dígitos do `infNFe/@Id`), nº/série, emissão, emitente (CNPJ, razão, UF), destinatário e o bloco `ICMSTot` inteiro (vNF, ICMS, ST, IPI, PIS, COFINS, frete, desconto, bases). **Antiduplicidade** pela chave, olhando `compras_xml` **e** `compras_cmv` (uma compra pode ter sido criada à mão com a chave). O modal tem **duas etapas**. **Etapa 1 (conferência)**: lista as notas com selo *nova*/*já importada*, soma o total, pede a **data de entrega** do lote e faz a pergunta que decide o resto — *"o valor pago é o mesmo da nota?"*. **Sim** importa direto com `valor_pago = valor_nfe`; **Não** abre a **etapa 2**, com um campo **vazio** por nota pra digitar o valor realmente pago (em branco = pagou o da nota) e a linha de **motivo** aparecendo só quando há diferença. Assim o caso comum é um clique e a exceção fica explícita, em vez de um campo já preenchido que se aceita sem olhar. O motivo vai pra `motivo_alteracao`, com `valor_alterado=true` e `diferenca_nfe`.
    - **Data de entrega** vale para o lote inteiro (`data_entrada`). Preenchida e **não futura** já grava a compra como `entregue`; em branco ou no futuro fica `pendente` — ainda vai ser conferida na chegada. Ao confirmar, grava em sequência **`compras_cmv` → `compras_xml` → `auditoria_xml_importacao`**; `valor_total` da compra é o **valor pago**, e `valor_nfe` guarda o da nota.
    - **Sem bucket de Storage**: o XML original inteiro vai pra `compras_xml.xml_original` (coluna text que já existe), com SHA-256 em `hash_xml`. Motivo: o backend só fala PostgREST (`/rest/v1`), não a Storage API — abrir um proxy novo pra guardar ~30 KB de texto não se paga. Por isso `compras_cmv.xml_url` fica nulo nas importações feitas por aqui.
    - **Sem transação** (PostgREST não tem): cada passo confere o anterior. Se o XML falhar depois da compra entrar, a compra permanece e o erro é reportado — apagar uma compra que já pode ter sido conferida seria pior.
    - Escrita: `WRITE_ALLOW` += `compras_xml`, `auditoria_xml_importacao`.
  - **XML Fiscal**: as NF-e do período (filtro mês/ano/empresa) com valor, chave e status do XML; baixa nota a nota ou **tudo em ZIP** (JSZip por CDN, mesmo padrão do SheetJS na tela de Vendas — o ZIP é montado no navegador a partir do `xml_original` do banco). Nota sem XML guardado fica de fora e o aviso diz quantas.
  - **NFE Entradas**: consulta das notas com busca livre (nº, chave, emitente, CNPJ, destinatário) e filtro de empresa; marca *ST* e *sem compra*. O olho abre o detalhe da nota — chave, CNPJ das duas pontas, nota × pago, motivo da diferença, impostos, quem importou, hash e o XML na íntegra.
  - **Relatórios NFE**: total das notas, emitentes distintos, notas com ST, soma de cada imposto (ICMS, ST, IPI, PIS, COFINS, frete, desconto — vindos da **compra** vinculada pela chave, já que `compras_xml` não guarda imposto), notas por mês e ranking por emitente.
  - **Conciliação**: as três divergências que importam — nota com valor diferente do pago (com o motivo registrado ao lado), NF-e importada **sem compra**, e compra com chave de NF-e **sem o XML guardado**.
  - **Fechamento**: grade dos 12 meses do ano com compras, total, quebra por empresa, NF-e, compras sem nota e entregas pendentes. Um mês só aparece como **fechado** quando toda compra foi entregue e toda compra tem NF-e — é conferência de compras, não o fechamento contábil (esse continua na tela de Fechamento Mensal).
  - **Acesso Contador**: o pacote do mês — cards de ICMS/ST/IPI/PIS+COFINS, tabela das notas com chave completa e CNPJ, **CSV** e **ZIP dos XMLs** do período. Só dado fiscal: nada de margem ou CMV, que não é o que o contador pede.
    - **Recorte por empresa** (filtro mês/ano/**empresa**): cada CNPJ tem contador próprio, então tabela, CSV, ZIP e PDF saem todos filtrados pela empresa escolhida, e o nome do arquivo leva o sufixo dela (`xmls-contador-2026-08-unifull.zip`). Em branco = todas.
    - **Comprovantes inseridos no período**: segunda tabela com o que tem arquivo anexado (`compras_cmv.xml_url`) — aluguel, água, luz, NFS-e, CT-e —, com categoria, empresa, fornecedor, valor e link pro arquivo. É o que o contador **não** recebe pelo XML, porque não tem chave de acesso. O botão **"Comprovantes do mês"** monta o ZIP baixando cada arquivo pela rota `/api/financeiro/arquivo/*` (bucket é privado); o nome dentro do ZIP é `AAAA-MM-DD-<nome original>` (o carimbo de tempo do caminho sai), e arquivo que falhar não derruba o pacote — sai listado no aviso do fim.
    - **Relatório fiscal (PDF)** no mesmo recorte, pelo `montarFiscalPDF()` compartilhado com a aba Relatório Fiscal — um gerador só, dois pontos de entrada.

- **Gerador de Pedido** (`pages/financeiro-pedidos.html`, nav `Gerador de Pedido`): emite o **documento de pedido em PDF**, personalizado por empresa. Três abas: **Pedido**, **Cadastro de fornecedor** e **Lista de contatos**. Lê/grava `pedido_empresas` (empresas emitentes), `pedidos` (histórico), `pedido_fornecedores` e `pedido_contatos`. **Não faz parte do clone do Readdy** — é ferramenta nova do usuário.
  - **Empresas emitentes**: cadastro fica **dentro desta página** (modal "Editar empresas"), não em tela separada. Campos: nome/razão social, **CNPJ**, **UF** e uma cor (usada no cartão da tela e na faixa do PDF). Na 1ª carga as 3 do negócio são semeadas automaticamente — **MULTIMIX VENDAS ONLINE**, **R SOUZA VENDAS ONLINE**, **TOP MIX** — e a partir daí são editáveis. Remover uma empresa **desativa** (`ativa=false`), nunca apaga: pedidos antigos referenciam o id.
  - **Itens**: código, descrição, **caixas fechadas**, **un/caixa**, **quantidade** e valor unitário — nessa ordem, que é a ordem em que o pedido é montado na prática. Preencher caixas + un/caixa **preenche a quantidade sozinha** (`caixas × un/caixa`) e escreve no campo; dá pra digitar por cima (produto avulso, sem caixa) e o valor digitado vale até mexer nas caixas de novo. A **quantidade É o total de unidades** do item — o valor unitário multiplica ela, e as caixas não somam nada por cima. Total do item = quantidade × valor unitário. Linha em branco nunca entra no documento.
  - **Numeração** `NNNN/AAAA` sugerida automaticamente, sequencial **por empresa dentro do ano** (continua do maior já salvo); digitar por cima desliga a sugestão.
  - **Aba "Pedidos criados"** (4ª aba, consulta): é onde os pedidos salvos vivem — saíram do fim do formulário. Busca livre (número, fornecedor, CNPJ, empresa, observações e **código/descrição dos itens**), filtro de empresa e de período (mês atual / últimos 30 dias / ano atual, pelo campo `data` do pedido, que é a data do documento), e totais de pedidos/unidades/valor acompanhando o filtro. O rótulo da aba traz o total de pedidos. **Salvar um pedido joga direto nessa aba**, com os filtros limpos e a linha nova marcada como *novo* — assim o pedido recém-criado nunca some atrás de um filtro.
  - **Pedidos salvos**: histórico com reabrir no formulário, regerar PDF e excluir. Os itens são gravados em **`itens jsonb`** na própria linha do pedido — sem tabela filha: o pedido é documento imutável, e um insert só evita N chamadas PostgREST (ver `decisions.md`).
  - **Snapshot da empresa**: `empresa_nome`/`empresa_cnpj`/`empresa_uf` são copiados pra linha do pedido no momento de salvar — reimprimir um pedido antigo mostra o CNPJ que valia naquele dia, não o atual.
  - **PDF**: janela de impressão do navegador (mesmo padrão de DRE/Recebíveis, sem lib de PDF). Layout A4 retrato com faixa colorida na cor da empresa, selo do número/data, cartões Emitente/Fornecedor, tabela zebrada com badge de caixas, painel de totais e linhas de assinatura. `print-color-adjust: exact` é obrigatório — sem isso o navegador imprime as faixas em branco.
  - **Funciona antes de existir a tabela**: se `pedido_empresas` responder erro (tabela ausente), a tela cai nas 3 empresas padrão em memória, avisa no topo e mostra o SQL de criação dentro do modal. O gerador de PDF nunca depende do banco. Assim que a tabela existir, a carga seguinte religa a gravação sozinha (sem F5).
  - **Aba "Cadastro de fornecedor"** (`pedido_fornecedores`): razão social, nome fantasia, CNPJ, inscrição estadual, pessoa de contato, telefone/WhatsApp, e-mail, endereço, cidade, UF, CEP, condição de pagamento, prazo de entrega, pedido mínimo e observações. Lista em cards com busca (nome/fantasia/CNPJ/contato/cidade/telefone/e-mail), editar, "usar no pedido" e remover — remover **desativa** (`ativo=false`), porque pedidos antigos referenciam o id.
  - **Botão WhatsApp**: ao lado do telefone no formulário (habilita/desabilita conforme o número digitado) e em cada card. `waNumero()` normaliza pra E.164 — tira máscara, remove prefixo `00` e completa o `55` quando vem só DDD + número; número fora de 12-15 dígitos é recusado com aviso, em vez de abrir um `wa.me` quebrado. Link oficial `https://wa.me/<numero>?text=…`, sem API nenhuma. Na aba Pedido, **"Enviar pedido no WhatsApp"** manda o resumo (número, itens, unidades, total, observações) pro fornecedor selecionado — complemento do PDF, não substituto: o documento formal continua sendo o PDF.
  - **Vínculo com o pedido**: o select de fornecedor cadastrado preenche nome e CNPJ (os campos seguem editáveis — dá pra ajustar a razão social num pedido específico sem mexer no cadastro). O pedido guarda só `fornecedor_id`; contato/endereço **não** são duplicados na linha, vêm do cadastro na hora de imprimir (o PDF mostra A/C do contato, telefone, e-mail e endereço). Diferente do snapshot da empresa emitente, que é copiado — ali o dado é do documento; aqui é agenda de contato, e o atual é o certo.
  - Se `pedido_fornecedores` não existir (foi adicionada depois das outras), a aba fica vazia e o pedido continua funcionando normalmente.
  - Escrita: `WRITE_ALLOW` += `pedido_empresas`, `pedidos`, `pedido_fornecedores`. **As tabelas precisam ser criadas no Supabase** (SQL no modal da própria página).
  - **RLS**: tabela nova neste projeto Supabase nasce com **RLS ligado e sem policy** — a leitura volta vazia **em silêncio** e a gravação responde `401 new row violates row-level security policy`. O script do modal já termina com o bloco que liga o RLS e cria a policy `app_acesso_total` (`for all to anon, authenticated`) nas 4 tabelas, igual às tabelas antigas do banco (`compras_cmv`, `compras_xml`…). Criar tabela nova aqui sem policy = tela que "não salva" sem explicar por quê.

Schema real das tabelas do ERP (via Readdy): `sales_entries`, `expenses`, `expense_categories`, `boletos_mensais`, `receivables`, `cartoes`, `cash_flow_entries`, `compras_cmv`, etc. — próximas telas: **Compras** (`compras_cmv`), **Home** (10 tabelas: sales_entries, cash_flow_entries, boletos_mensais, expenses, receivables, monthly_goals, daily_alerts, daily_alert_logs, compras_xml, backups).

## Módulo Inteligência de Negócio (BI) — dados operacionais (Postgres principal)

Diferente do Financeiro (que lê do Supabase), o BI analisa os **dados operacionais** do Postgres principal (`orders`, `items`, etc.). Só admin (MODULES `bi`, prefixo `/api/bi`). Amazon fica **de fora** dos números (sandbox/mock inflava — mesma regra do `dashboard/kpis`).

- **Rota** `server/src/routes/bi.js`, montada em `/api/bi` no `server.js`. Espelho em `js/db.js`: `getBiPainel(period, store_id)`.
- **`GET /api/bi/painel?period=30&store_id=`** — payload único do Painel Estratégico: `kpis` (receita/pedidos/ticket/unidades + `receita_ant`/`pedidos_ant` e `cresc_receita`/`cresc_pedidos` = crescimento % vs. período anterior de mesma duração), `diaria` (série por dia), `canais` (ML/Shopee, receita/pedidos), `top` (top 10 produtos por receita), `clientes` (novos vs. recorrentes — "novo" = 1ª compra histórica caiu na janela). Filtra pedidos não-cancelados por data (SP), exclui Amazon, `store_id` opcional.
- **Página** `pages/inteligencia-negocio.html` (nav `Painel Estratégico`, layout `js/layout-bi.js`): tema escuro, Chart.js. Filtros período (7/15/30/90) + loja; 4 KPIs com badge de crescimento; gráfico combo receita(barra)+pedidos(linha); comparativo por canal (donut) e clientes novos×recorrentes (donut); tabela top 10 produtos. Accent roxo (#8b5cf6).
- **Resumo por Venda** (`pages/bi-vendas.html`, nav `Resumo por Venda`): cada pedido em formato **card** (foto, badges de logística, breakdown financeiro) em vez de linha de tabela — pedido do usuário, inspirado na tela nativa "Imprimir Etiquetas" do Mercado Livre. **Reusa `GET /api/vendas/detalhado`** (mesmo endpoint da tabela em `pages/vendas.html`/Operacional — zero lógica de margem duplicada, mesma fórmula de `finance.md`) filtrando por loja/status/período/busca; um botão "Mostrar mais" pagina no cliente de 24 em 24 (a rota já limita a 1000 linhas). **Toggle "cobrar imposto em vendas Flex"** na barra de filtros (`GET`/`PATCH /api/config/imposto-flex`) — flag geral, desligada por padrão (vendas Flex não têm nota fiscal); afeta também Inteligência de Margem, ver `business-rules.md`. **Edição manual de Tarifa** (✏️ no campo Tarifa do card, `PATCH /api/vendas/:orderId/tarifa`) — escape hatch por pedido pra quando a Conciliação vem errada; card mostra "editado manualmente" e um botão de desfazer quando ativo. **Edição manual de Frete Vendedor** (✏️ no campo "Frete vend.", `PATCH /api/vendas/:orderId/frete`, v86) — mesmo padrão da tarifa, precedência independente, vence até a substituição do custo motoboy Flex. **Toggle "custo motoboy Flex"** (`GET`/`PATCH /api/config/frete-motoboy`, v85) — flag geral + valor editável (padrão R$12,90), desligada por padrão; quando ativa, substitui o frete do vendedor pelo custo real de motoboy terceirizado (RR Express/Pex Entregas) nas vendas Flex, descontado o que o ML de fato reembolsa; card mostra 🛵 no label "Frete vend." quando aplicado naquela venda; afeta também Inteligência de Margem, ver `business-rules.md`. **Badge de Margem em 6 faixas de cor por MC%** (verde escuro/verde/amarelo escuro/laranja/vermelho/vermelho escuro — `margemTier()`, ver `business-rules.md`), mesma escala nos KPIs "MARGEM"/"MC %" do topo.
  - **Campos novos na SELECT** (extraídos de `orders.raw_data`, que já guarda o payload cru do pedido — sem migration): `buyer_first`/`buyer_last` (nome real do comprador, nem sempre preenchido pelo ML), `pack_id` (nº do carrinho — só aparece se diferente do nº da venda), `sku` (best-effort: `seller_sku` do item do pedido, senão `seller_custom_field`). Mais `i.thumbnail` e `i.available_quantity` (join com `items`).
  - **Honestidade dos dados que a tela NÃO tem** (avisado no rodapé da página, não fabricado): **estoque mostrado é o ATUAL** (`items.available_quantity`), não o saldo no momento da venda — esse histórico não é guardado em lugar nenhum. **CEP do comprador não é capturado** — `/orders/:id` não traz endereço; estaria em `/shipments/:id` (`receiver_address`), que hoje só grava status/data, não o payload completo (ver `handleShipment`, `worker.js`).
  - Botões de copiar (venda #, carrinho #, SKU, MLB) — `navigator.clipboard`, mesmo padrão de outras telas com dado pra colar em outro lugar.
  - Filtro de período tem **"Hoje"** (`days=0`, aproveitando o mesmo `CURRENT_DATE - $3::int` de `/vendas/detalhado` — sem coluna/parâmetro novo) **como opção padrão ao abrir a página**, além de 7/30/60/90 dias.
  - **Custo editável no card** (✏️ ao lado de "Custo"): grava direto em `items.cost` via `PATCH /items/:id/custo` (por `ml_id`) — **não** usa `/custos/:sku`, que tem a ambiguidade sku×ml_id documentada abaixo. Como o custo é por anúncio (não por venda), salvar atualiza **todos os cards já carregados do mesmo MLB**, não só o clicado. Os KPIs do topo são ajustados **pelo delta local** (soma só sobre pedidos não-cancelados, igual ao agregado do backend) em vez de recarregar a página — recarregar bateria no cache de 60s de `/vendas/detalhado` e o resumo ficaria com o valor velho por até 1 minuto, mais confuso que não atualizar imediatamente.
  - **Atualizar tarifa/frete na hora** (🔄 no card, `POST /api/vendas/:orderId/atualizar`): venda de "Hoje" costuma mostrar Tarifa/Frete vend. zerados — não é bug, é que a Conciliação oficial (relatório do Mercado Pago) só fecha no dia seguinte, e o webhook de pagamento/envio pode não ter chegado ainda. O botão refaz **na hora**, pra aquele pedido só, o que os webhooks fariam: `/orders/:id` (ml_fee + `raw_data` inteiro — também atualiza buyer/pack_id/sku do card), `/shipments/:id/costs` (frete vendedor) e `/collections/:id` (tarifa real, upsert em `ml_payments`) — mesma lógica de `handleOrder`/`handleShipment`/`handlePayment` do `worker.js`, **replicada** em `routes/api.js` (não importa `worker.js`: ele inicia filas/crons ao carregar, arriscado dentro do processo HTTP — mesmo motivo do botão "Reprocessar" da Conciliação Bancária). Ação pontual explícita de rota, permitida pela regra de arquitetura #3 (`mlClient.js` fora de leitura de listagem).
    - **SQL compartilhado**: o SELECT com a precedência de taxa (Conciliação → `ml_payments` → pedido) e a fórmula de margem (`calcularMargemLinha`) foram extraídos de dentro de `/vendas/detalhado` pra constantes reusadas pelas duas rotas — nunca duas fórmulas de margem no mesmo arquivo.
    - A linha volta pronta do backend (já recalculada) e troca **só aquele card** no cliente; os KPIs do topo ajustam pelo delta de margem (só se o pedido não estiver cancelado), sem esperar o cache de 60s de `/vendas/detalhado` (que continua, de propósito, sem invalidação — ver comentário na rota e `decisions.md`).
    - Pedido novíssimo sem pagamento ainda: a tarifa segue no nível 3 (`orders.ml_fee`) até o pagamento existir — não é erro, `payment_error`/`shipping_error` só logam no console quando a API do ML realmente falha (ex.: logística sem custo exposto).
  - **Módulo BI**: página registrada em `BI_NAV_ITEMS` (`js/layout-bi.js`) e em `MODULES.bi.pages` (`server/src/routes/staffAuth.js`) — sem o 2º passo a página ficaria acessível por URL direta a qualquer papel logado, mesmo não aparecendo no menu (ver "Autorização de módulos" no topo deste arquivo).
- **Inteligência de Margem** (nav `Inteligência de Margem` → `pages/bi-margem.html`, `GET /api/bi/margem`): analista financeiro **determinístico** (motor em JS puro) + camada de **IA sob demanda** (Fase 2, LLM só interpreta) sobre os anúncios que venderam no período — margem, custo, frete, tarifa, imposto por produto, cruzado com estoque/ruptura. Pedido explícito do usuário: nunca inventar número, sempre separar dado real de recomendação.
  - **6 páginas próprias, não abas JS** (decisão desta tarefa — ver `decisions.md`): `bi-margem.html` (Visão Geral) · `bi-margem-produtos.html` · `bi-margem-portfolio.html` · `bi-margem-frete.html` · `bi-margem-estoque.html` · `bi-margem-acoes.html`. Cada uma faz seu próprio fetch de `GET /api/bi/margem?days&store_id` (payload único, sem endpoint por página) e renderiza só a sua fatia. Navegação entre elas por **2 caminhos**: a barra de abas no topo da própria página (`js/biMargemTabs.js`, `renderBiMargemTabs(paginaAtual)`) — links reais, cada um carregando `?days=&store_id=` da URL atual (`biMargemQueryAtual()`), então o filtro escolhido sobrevive à troca de página; e **as 6 têm entrada própria no menu lateral** (`BI_NAV_ITEMS`, `js/layout-bi.js`, pedido explícito do usuário — ver `decisions.md`) — atalho que sempre abre com filtro padrão, já que o menu é global e não conhece o filtro da página atual. TODAS as 6 estão em `MODULES.bi.pages` (`server/src/routes/staffAuth.js`) — sem isso ficariam acessíveis por URL direta sem o gate de módulo.
  - **Visão Geral** (`bi-margem.html`): filtro de período completo (Hoje/Ontem/7/14/30/60/90 dias/Mês atual/Mês anterior/personalizado, resolvido em `date_from`/`date_to` no cliente — `resolverPeriodo()`) + filtro de categoria (opções derivadas do próprio payload, sem lookup de nome — mesmo padrão de `qualidade-anuncio.html`) · **card "Saúde do Negócio"** (score 0-100 determinístico, 6 sub-scores — rentabilidade/crescimento/eficiência logística/saúde do estoque/concentração de portfólio/eficiência comercial, ver `business-rules.md`) · KPIs de lucro/MC%/faturamento com crescimento vs. período anterior de mesma duração · **card "Por que a margem mudou"** (decomposição EXATA da variação — soma de faturamento/custo/imposto/tarifa/frete comprador/frete vendedor bate com o delta de margem, mais indicador de mix de portfólio) · **card "O que mudou"** (8 destaques de maior alta/queda por anúncio: faturamento/margem/frete/tarifa/pedidos) · **card "Análise da IA"** (botão sob demanda, ver abaixo) · top 3 ações prioritárias · **card "Como cada score funciona"** (fim da página — os 6 sub-scores da Saúde do Negócio explicados, ordenados do pior pro melhor, com dica de melhoria genérica por sub-score, ver `business-rules.md`). Badges de contagem no menu de abas (`js/biMargemTabs.js`, `renderBiMargemTabs(pagina, contadores)`) mostram `skus_ruptura_iminente` em "Estoque & Ruptura" e `acoes.length` em "Ações Recomendadas", populados a partir do mesmo payload em TODAS as 6 páginas (não só na Visão Geral).
  - **Produtos** (`bi-margem-produtos.html`): tabela por anúncio com classificação/MC%/frete%/tarifa%/estoque/score, filtro por classificação, **aviso de amostra pequena** (< 3 pedidos no período — classificação/score podem não ser representativos), **sparkline de MC% das últimas até 6 semanas** (SVG inline, janela fixa de 42 dias independente do filtro de período), botão CSV, **colunas ordenáveis** (client-side, clique no cabeçalho). Clicar numa linha abre um **modal de detalhe** (`GET /api/bi/margem/produto/:itemId`, consulta dedicada por SKU, não reusa `computarMargem`): gráfico temporal diário (Chart.js, faturamento + MC%), **decomposição em cascata** (Venda→Tarifa→Frete→CMV→Imposto→MC), **simulador de preço com % livre** (100% client-side, mesma fórmula das ações REPRECIFICAR mas sem round-trip nem cenário fixo), e as ações recomendadas já calculadas filtradas por aquele `item_id` (reaproveitadas de `DADOS.acoes`, nunca recalculadas).
  - **Portfólio** (`bi-margem-portfolio.html`): % do faturamento por faixa de MC%, concentração top 10, **matriz do portfólio** (scatter/bubble Chart.js — X=volume, Y=MC%, raio=faturamento, cor=classificação; clique num ponto abre o modal de detalhe do produto, `js/biMargemProdutoModal.js`), **cenário de mix** (simulador client-side: desloca N p.p. do faturamento de baixa pra alta margem, mantendo faturamento total e MC% de cada grupo constantes — ver `business-rules.md`). Canibalização automática deliberadamente NÃO implementada (ver `decisions.md`).
  - **Frete & Tarifa** (`bi-margem-frete.html`): % da receita por tipo de envio e por conta, com destaque visual acima de 15%/20% · **ranking por produto** (MC antes de frete/tarifa vs. MC final, impacto em p.p., ordenável, 100% client-side sobre o payload já carregado) · **detecção de anomalia** (frete/tarifa de um produto bem acima da média do próprio grupo — logística ou conta — nunca da média geral, que misturaria perfis de custo diferentes).
  - **Estoque & Ruptura** (`bi-margem-estoque.html`): tabela ordenável por **dias de estoque** (padrão) ou por **Prioridade** (`prioridade_score`, ponderação urgência/margem/velocidade — ver `business-rules.md`) — produto de baixa margem e estoque baixo não fura a fila só por estar mais perto da ruptura. Colunas extras: **Valor em risco** (reaproveita o `impacto_estimado` da própria ação `REPOR_ESTOQUE` já calculada, nunca uma 2ª fórmula) e **Prioridade** (barra visual 0-100). KPIs: **capital parado** (Σ `estoque_atual × custo_unitario`, só SKUs que venderam no período — mesma limitação de `dias_estoque`), anúncios em ruptura iminente, valor total em risco. Tabela dedicada **"Capital parado"** (top 30 por maior valor imobilizado, exclui SKU sem custo conhecido). `custo_unitario` vem do backend (`computarMargem`, `routes/bi.js`) derivado da linha de venda mais recente do SKU (não lê `items.cost` de novo).
  - **Ações Recomendadas** (`bi-margem-acoes.html`): ranking Top 10 por impacto estimado, cada uma com Problema/Evidência/Ação/Impacto/Esforço/Confiança/**Premissa**; ação `REPRECIFICAR` traz **3 cenários** (+3%/+5%/+10% de preço, cada um com impacto estimado próprio — `impacto_estimado` do topo continua sendo o cenário de 5%, por compatibilidade/ordenação); botão CSV (monta a matriz na mão, ações são cards, não `<table>`). **Fase F**: KPIs (impacto total priorizado — com exclusividade mútua, ver `business-rules.md` — total de ações, pendentes/em andamento, concluídas) · **matriz Impacto × Esforço** (Chart.js scatter, X=esforço categórico BAIXO/MEDIO/ALTO, Y=impacto_estimado em R$, cor=prioridade, clique abre o modal de detalhe do produto — `js/biMargemProdutoModal.js`, mesmo componente de Produtos/Portfólio) · **status manual por ação** (Pendente/Em andamento/Concluída/Descartada + nota livre, persistido em `business_insights` via `PATCH /api/bi/margem/acoes-status`, carregado com `GET` no mesmo `carregar()`) · toggle "ocultar concluídas/descartadas". Status é só o que a analista marcou — **sem** feedback loop automático (medir resultado real pós-ação), decisão deliberada, ver `decisions.md`.
  - **Camada de análise ANTES de qualquer IA**: banco → agregação por anúncio (reusa `VENDA_DETALHE_SELECT`/`calcularMargemLinha` de `server/src/vendaMargem.js` — mesmo módulo usado por `routes/api.js` e `financeService.js`, nunca duplicado) → classificação/score/ações/decomposição de causa/tendência semanal, tudo em JS puro dentro de `computarMargem(days, storeId)` (`routes/bi.js`). Todos os limiares (MC baixa/alta, corte de "alto faturamento" por Curva ABC, faixas de ruptura, amostra mínima, cenários de reprecificação) estão documentados em `business-rules.md` — não são "opinião de IA", são regras explícitas e ajustáveis.
  - **Fase 2 — Análise da IA** (`POST /api/bi/margem/narrativa`, `server/src/ai/margemNarrativa.js`): sob demanda (botão "Gerar análise deste período" na Visão Geral, nunca automático — tem custo de token), reusa `computarMargem()` (nunca recalcula margem) e manda pro Haiku um contexto REDUZIDO (resumo + até 15 produtos críticos + 5 estrela + 10 em ruptura + 8 ações — nunca o portfólio inteiro, por custo). Devolve `{ resumo_executivo, causa_raiz, o_que_eu_faria_agora[] }` em JSON. 503 se `ANTHROPIC_API_KEY` não estiver configurada (mesmo padrão de `routes/analise.js`). **Sem persistência** — cada clique gera uma análise nova, não fica salva (tabela de histórico/feedback loop segue como Fase 3, ver `todo.md`).
- **Vendas por Estágio** (`bi-rankeamento.html`, nav `Vendas por Estágio`, `GET /api/bi/rankeamento`): integração com o módulo **Rankeamento** (Operação, `rankeamento.md`) — cruza o estágio ATUAL de cada anúncio (`rankeando`/`ranqueado`/`monitoramento`/`recuperacao`, fonte única `ranking_ads`/`ranking_ad_links`) com as vendas/margem do período. Responde "como cada estágio do processo de ranqueamento está performando em vendas". **Filtro de período**: `days` (7/14/30/60/90), "Hoje" (resolve sozinho com `days=1`, sem precisar de datas) ou "Período personalizado" (`date_from`/`date_to`, mesmo contrato de `resolverPeriodo()` de `bi-margem.html`) — pedido explícito do usuário, ver `business-rules.md`. Visão executiva (estágio líder de hoje + score) · 4 blocos clicáveis por estágio · tabela de comparação ordenável (inclui bucket `sem_rankeamento`) · evolução temporal por estágio (Chart.js) · hoje vs. ontem vs. média 7 dias · ranking "quem vendeu mais/menos hoje" · lista de vendas de hoje · anúncios do estágio selecionado (com aviso de "fora do padrão" do próprio estágio) · Recuperação antes×depois (efeito observado, nunca causal) · insights automáticos (texto templated, sem LLM). Estágio da venda é sempre o ATUAL do anúncio — `ranking_ads` não guarda histórico confiável de mudança de fase, então não há reconstrução histórica (ver `decisions.md`/`business-rules.md`). **Tag de estágio também em `bi-vendas.html`** (Resumo por Venda): cada card mostra o estágio atual do anúncio vendido (busca em lote via `POST /api/ranking/fase-lote`, 1 request pra tela toda), mesma paleta/label de `pages/rankeamento.html`.
- **Agente Financeiro** (`bi-nav` → `pages/agente-financeiro.html`, `routes/agenteFinanceiro.js` em `/api/bi/agente-financeiro`): 4 relatórios **sob demanda** (botão — pedido explícito do usuário, nunca chat livre) que cruzam o Postgres operacional (Inteligência de Margem) com o Supabase do Financeiro. **Mora no módulo BI, não no Financeiro** — decisão do usuário (o agente lê os dois bancos e cruza na aplicação; ver `decisions.md`). Camada 100% determinística nova, `server/src/financeiroCalc.js`, extraída das fórmulas que já rodavam só no `<script>` de `financeiro-despesas.html` (DRE/break-even) e `financeiro-projecao-caixa.html` (fluxo/projeção) — mesma fórmula, nunca duplicada; as duas páginas do Financeiro continuam com sua própria cópia no frontend por ora (não foi arriscado mexer nelas nesta tarefa — ver `decisions.md`). A IA (`server/src/ai/agenteFinanceiro.js`, mesmo padrão "só interpreta" de `margemNarrativa.js`) nunca calcula nada, só lê o JSON pronto; falha da IA não derruba o relatório (`ia_erro` no payload, dados continuam válidos).
  - **DRE do mês** — receita, deduções automáticas de venda, despesas cadastradas, lucro líquido, ponto de equilíbrio, comparado ao mês anterior.
  - **Fluxo de caixa + projeção** (7/15/30/60 dias) — caixa atual, entradas/saídas previstas, saldo projetado, em quantos dias fica negativo se ficar.
  - **Contas a pagar** — boletos + faturas de cartão pendentes, vencidos/próximos 7d/30d, por empresa.
  - **Cruzamento ML × Financeiro** — junta `computarMargem` (margem real das vendas ML, Postgres) com `sales_entries` do mesmo período (Supabase) e aponta a diferença de faturamento/margem entre o que o ML realmente vendeu e o que foi lançado manualmente em Vendas & Custos.
  - `computarMargem` (antes só interno a `routes/bi.js`) foi pendurado como propriedade do `router` exportado (`module.exports.computarMargem`) especificamente pra isso — nunca uma 2ª fórmula de margem no projeto.
  - Página liberada em `MODULES.bi.pages` (`staffAuth.js`) e `BI_NAV_ITEMS` (`js/layout-bi.js`) — mesmo admin-only do resto do BI.

- **Próximas telas do BI** (menu vazio ainda): Curva ABC (BI-específica, a existente é do módulo Operacional), Desempenho de produtos (visitas→conversão/qualidade/buy-box), Análise de clientes detalhada, Tendências/sazonalidade.
